/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["img.clerk.com", "c.saavncdn.com", "www.jiosaavn.com", "pli.saavncdn.com"], // Add the domain hosting the image
  },
};

export default nextConfig;
